version: "3.7"

services:
    runbotics-sap-actions:
        build:
            context: "."
            dockerfile: "./Dockerfile"
            args:
                EXTENSION_NAME: "runbotics-sap-actions"
        volumes:
            - "~/volumes/runbotics/runbotics-extensions/:/runbotics-extensions"